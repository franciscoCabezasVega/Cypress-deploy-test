{
  "name": "cypress-deploy-test",
  "version": "1.0.0",
  "description": "This project aims to show the use of the cypress tool, as a test automation tool where you can easily run the tests corresponding to the system with the various use cases required.",
  "main": "cypress.config.js",
  "devDependencies": {
    "@faker-js/faker": "^7.6.0",
    "cypress": "^12.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
